# App deps (torch / torchvision / dlib installed separately by install.ps1)
Django>=4.2,<5.0
Pillow
opencv-python==4.10.0.84
ultralytics==8.3.40
numpy
scipy
matplotlib
tqdm
onnx
onnxruntime
# Optional GPU ORT (install instead of onnxruntime when CUDA available):
# onnxruntime-gpu
