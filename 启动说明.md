# 疲劳驾驶检测系统启动说明

## 系统概述

本系统基于YOLO和dlib的疲劳驾驶检测系统，能够实时检测驾驶员的面部状态、疲劳程度和不良驾驶行为（如打哈欠、抽烟、打电话等）。系统采用Django框架开发，支持实时摄像头检测和视频文件检测两种模式。

## 环境要求

- **Python 版本**: 3.8.10
- **操作系统**: Windows/macOS/Linux
- **主要依赖**:
  - Django 4.2
  - OpenCV 4.7.0
  - dlib 19.24.0
  - ultralytics 8.0.0 (YOLO模型)
  - torch/torchvision (深度学习框架)
  - numpy, scipy (数据处理)

## 启动步骤

### 1. 准备环境

1. **确认Python环境**:
   ```bash
   python --version
   ```
   确保版本为3.8或更高。

2. **激活虚拟环境** (如使用Anaconda/venv):
   ```bash
   # 如果使用Anaconda
   conda activate yolov5  # 或者您创建的环境名称
   
   # 如果使用venv
   source venv/bin/activate  # Linux/macOS
   # 或
   venv\Scripts\activate  # Windows
   ```

### 2. 检查权重文件

确保以下权重文件已放置在正确位置:

- YOLO模型权重: `/weights/best.pt`
- dlib面部特征点检测器: `/weights/shape_predictor_68_face_landmarks.dat`

如未放置，请从以下链接下载:
- dlib特征点检测器: http://dlib.net/files/shape_predictor_68_face_landmarks.dat.bz2

### 3. 启动服务器

1. **切换到项目根目录**:
   ```bash
   cd /您的路径/基于YOLO11的疲劳驾驶检测系统/fatigue_detection_system
   ```

2. **启动Django服务器**:端口不是8000，就是8001
   ```bash 
   python manage.py runserver 8001
   ```
   系统将在8001端口启动，如需使用其他端口，请修改端口号。

3. **开始使用**:
   - 在浏览器中访问: `http://127.0.0.1:8001/`
   - 使用系统账号登录，或注册新账号

### 4. 声音文件配置

系统使用自定义声音文件进行警报提示，这些声音文件包括:
- `alert.mp3`: 基本警报声
- `fatigue_alert.mp3`: 疲劳警报声
- `phone_alert.mp3`: 打电话警报声
- `smoking_alert.mp3`: 抽烟警报声

声音文件已通过代码生成并存放在`static/sounds/`目录下，无需额外配置。

## 系统功能使用

1. **实时检测**: 
   - 点击仪表盘上的"开始检测"按钮
   - 允许摄像头权限
   - 系统将实时分析驾驶员状态并显示检测结果

2. **视频检测**:
   - 点击"视频检测"功能
   - 上传视频文件
   - 系统将分析视频并生成检测报告

3. **检测结果查看**:
   - 在"历史记录"页面查看所有检测会话
   - 点击具体会话可查看详细检测结果

4. **系统配置**:
   - 在"系统配置"页面可调整检测参数、警报阈值等设置

## 常见问题解决

1. **无法启动服务器**:
   - 检查是否位于正确目录下(包含manage.py的目录)
   - 确认已激活Python虚拟环境
   - 检查Django是否正确安装

2. **检测无反应**:
   - 确认摄像头权限已授予浏览器
   - 检查YOLO和dlib模型权重文件是否存在
   - 查看浏览器控制台是否有错误信息

3. **无法播放警报声音**:
   - 确保声音文件路径正确
   - 检查浏览器是否允许自动播放音频
   - 检查系统声音是否正常工作

如有更多问题，请参考系统日志或联系系统管理员。

## 备注
系统会自动记录所有检测会话和结果，并在数据库中存储，以便后续分析和统计。管理员可通过Django管理后台(`http://127.0.0.1:8001/admin/`)查看和管理所有数据。


感谢使用疲劳驾驶检测系统！
