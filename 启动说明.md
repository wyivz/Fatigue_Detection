# 启动说明

详细安装与功能说明见根目录 [README.md](README.md)。

## 本机最短路径（Windows）

1. 激活 `venv`（或先按 README 创建环境并安装依赖）
2. 确认权重存在：
   - `fatigue_detection_system/weights/best.pt`
   - `fatigue_detection_system/weights/shape_predictor_68_face_landmarks.dat`
3. 仓库根目录运行 `start.bat`，或：

```bash
cd fatigue_detection_system
python manage.py migrate
python manage.py runserver 127.0.0.1:8000
```

4. 浏览器打开 http://127.0.0.1:8000/

## GigE 工业相机

先安装与开发机一致的海康 MVS Runtime，并关闭 MVS 客户端预览后再在网页中选择 GigE 设备。详见 `deploy/MVS_SETUP.txt`。

## 警报音

自定义音效应放在 `fatigue_detection_system/static/sounds/`（可用 `generate_sounds.py` 生成占位文件）。
